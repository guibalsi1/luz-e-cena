import MenuList from '../../Menu/MenuList'
import MenuItem from '../../Menu/MenuItem'

const HeaderLinks = () => {
  return (
    <MenuList>
        <MenuItem>Events</MenuItem>
        <MenuItem>Plus Club</MenuItem>
        <MenuItem>About Us</MenuItem>
    </MenuList>
  )
}

export default HeaderLinks